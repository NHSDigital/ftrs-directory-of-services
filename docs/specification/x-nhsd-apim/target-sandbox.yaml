type: hosted
healthcheck: /_status
containers:
  - name: uec-dos-ingestion
    image:
      name: uec-dos-ingestion
      tag: latest
    environment:
      LOG_LEVEL: info
      NODE_ENV: development
