[pytest]
addopts = --html=report.html -s --self-contained-html --maxfail=1 --disable-warnings -q
testpaths = api-tests/tests/features
markers =
    api: Marks API tests (deselect with '-m "not api"')
    ui: Marks UI tests (deselect with '-m "not ui"')