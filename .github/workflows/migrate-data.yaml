name: Migrate data to dynamodb
# This workflow manages DynamoDB backups by deleting old backups and creating new ones.
# TODO workspace is temporary for testing -
permissions:
      id-token: write
      contents: read
on:
  workflow_call:
    inputs:
        environment:
          description: "The relevant github environment (for secrets and variables)"
          required: true
          type: string
        function_name:
          description: "The name of the AWS Lambda function to trigger data migration."
          required: true
          type: string
        type:
          description: "The type of permissions (e.g., account, app)"
          required: true
          type: string

jobs:
  migrate-data-via-etl:
    name: "Trigger etl data migration in ${{ inputs.environment }}"
    timeout-minutes: 15
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Install jq"
        run: sudo apt-get install -y jq

      - name: "Configure AWS Credentials"
        uses: ./.github/actions/configure-credentials
        with:
          aws_account_id: ${{ secrets.ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          type: ${{ inputs.type }}

      - name: "Trigger data migration"
        id: "trigger_migration"
        shell: bash
        run: |
          export FUNCTION_NAME=${{ inputs.function_name }}
          /bin/bash ./scripts/workflow/trigger-data-migration-lambda.sh

      - name: "Poll data migration queue"
        id: "poll_data_migration_queue"
        shell: bash
        run: |
          export ENV="${{ inputs.environment }}"
          /bin/bash ./scripts/workflow/poll-data-migration-queue.sh
