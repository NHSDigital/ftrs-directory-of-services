[tool.semantic_release]
version_source = "tag"
branch = "main"
upload_to_pypi = false
build_command = ""

[tool.semantic_release.prerelease]
branches = ["task/*"]
identifier = "pre"
bump_type = "patch"
