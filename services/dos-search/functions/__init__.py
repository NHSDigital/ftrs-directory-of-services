"""dos-search shared namespace.

`functions` is included in Lambda zips as a top-level package.

- Endpoint-specific handlers live under `functions/<lambda_name>`.
- Shared code lives under `functions/libraries`.
"""
