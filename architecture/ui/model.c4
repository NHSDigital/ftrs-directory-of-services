model {

  container ui {
    title "DoS UI"
    description 'React application, deployed as a SPA'
    style {
      icon tech:react
      shape browser
    }
  }

  dosLead -> ui "opens in browser"
  ui -> cis2 "Logs in with"
  ui -> apim.dosSearchProxy "Searches DoS"
  ui -> apim.dosIngestProxy "Updates and Queries DoS"

}
