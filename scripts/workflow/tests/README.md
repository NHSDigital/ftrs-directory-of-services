# Workflow script tests

This folder holds unit tests for repository-level workflow scripts under `scripts/workflow/`.

These scripts are used by CI/workflows and are not part of any individual service runtime, so their tests live outside service folders.
